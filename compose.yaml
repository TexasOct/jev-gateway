services:
  jev-gateway:
    build: .
    image: jev-gateway:local
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ${HOME}/.jev-gateway:/home/jev/.jev-gateway
    restart: unless-stopped
